<h1>Menu</h1>
  <h3>Welcome to the Food Menu</h3>
  <table>
    <thead>
      <tr>
        <th>Food Name</th>
        <th>Category</th>
        <!-- Add other table headers here if needed -->
      </tr>
    </thead>
    <tbody>
      {{#each menu}}
      <tr>
        <td>{{this.foodName}}</td>
        <td>{{this.category}}</td>
        <!-- Add other table data here if needed -->
      </tr>
      {{/each}}
    </tbody>
  </table>
  <br><br>
<a href="/user/edit-profile">Edit Profile</a>
